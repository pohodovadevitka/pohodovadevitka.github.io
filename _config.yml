# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Pohodová Devítka
email: bitesskybeh@seznam.cz
facebook_url: https://www.facebook.com/pohodovadevitka
twiter_url: https://twitter.com/pohodovadevitka
gallery_url: https://pohodovadevitka.rajce.idnes.cz
description: Pohodová Devítka je běžecký závod ve Velké Bíteši, který se pořádá v září, vždy po zdejších hodech. Hlavní trasa měří 9km, děti mohou běžet kratší trasy.
baseurl: "/" # the subpath of your site, e.g. /blog
url: "https://www.pohodovadevitka.cz" # the base hostname & protocol for your site, e.g. http://example.com
logo_large: http://www.pohodovadevitka.cz/images/logo.png

# Build settings
markdown: kramdown
plugins:
  - jekyll-feed
  - jekyll-sitemap

sass:
  sass_dir: css
  style: compressed

permalink: pretty

collections:
  vysledky:
    output: true
    permalink: /:collection/:year

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - Gemfile
  - Gemfile.lock
  - _bin
  - _sponsors_original
